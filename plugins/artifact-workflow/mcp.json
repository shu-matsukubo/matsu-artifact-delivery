{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/mcp.schema.json",
  "mcpServers": {
    "artifact-task-memory": {
      "type": "stdio",
      "command": "node",
      "args": ["${PLUGIN_ROOT}/mcp/task-memory.cjs"],
      "env": {
        "ARTIFACT_WORKFLOW_DATA_DIR": "${PLUGIN_DATA}/task-memory"
      }
    }
  }
}
